<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="create_table_users_roles" author="steam-clone">
        <comment>Создание таблицы связи пользователей и ролей</comment>
        
        <createTable tableName="users_roles">
            <column name="user_id" type="bigint">
                <constraints nullable="false"/>
            </column>
            <column name="role_id" type="bigint">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey tableName="users_roles" columnNames="user_id, role_id"/>

        <addForeignKeyConstraint
                baseTableName="users_roles"
                baseColumnNames="user_id"
                constraintName="fk_users_roles_user_id"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="CASCADE"
                onUpdate="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="users_roles"
                baseColumnNames="role_id"
                constraintName="fk_users_roles_role_id"
                referencedTableName="roles"
                referencedColumnNames="id"
                onDelete="CASCADE"
                onUpdate="CASCADE"/>

        <rollback>
            <dropTable tableName="users_roles"/>
        </rollback>
    </changeSet>

</databaseChangeLog>

